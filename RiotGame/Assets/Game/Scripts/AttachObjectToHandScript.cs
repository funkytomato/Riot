using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttachObjectToHandScript : MonoBehaviour
{
    public GameObject objectToAttach;

    private GameObject rightHand;

    // Start is called before the first frame update
    void Start()
    {

        //var userControl = FindObjectOfType<AirplaneUserControl>();
        //Rigidbody targetRb = userControl.GetComponent<Rigidbody>();
        //Debug.Log("Aircraft velocity: " + targetRb.velocity);

        

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
